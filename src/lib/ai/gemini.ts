import { google } from '@ai-sdk/google'

// Gemini 2.5 Flash-Lite — Standard for the application (User requested version)
export const geminiFlashLite = google('gemini-2.5-flash-lite')

// Recommended for synthesis and planning tasks
export const geminiFlash = google('gemini-2.5-flash-lite')

